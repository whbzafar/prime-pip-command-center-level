import React from 'react';

type Props = {
  account: any;
  metrics: any;
  currentUser: any;
  activeTab: string;
};

/**
 * The cinematic atmosphere was creating a large empty vertical stage above
 * the dashboard content. Keep the component/API in place so callers remain
 * stable, but render no layout so the dashboard starts immediately below
 * the navigation.
 */
export function CommandCenterAtmosphere(_props: Props) {
  return null;
}
